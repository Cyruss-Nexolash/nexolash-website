import React from 'react';

const ToggleGroup = ({ children }) => <div>{children}</div>;

const ToggleGroupItem = ({ children }) => <button>{children}</button>;

export { ToggleGroup, ToggleGroupItem };